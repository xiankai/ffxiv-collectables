import React, { Component, PropTypes } from 'react';

export default class Collectable extends Component {
	render() {
		return (
			<div>this is a collectable</div>
		);
	}
}

Collectable.propTypes = {
	
}